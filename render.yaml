services:
  - type: web
    name: cafe-backend
    env: node
    rootDir: backend
    buildCommand: npm install && npm run render-build
    startCommand: npm run start:prod
    envVars:
      - key: NODE_VERSION
        value: 20.16.0
      - key: DATABASE_URL
        sync: false
