{
  "name": "stitch-cafe-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm run postinstall --prefix backend || true",
    "build": "npm run build --prefix backend",
    "start": "npm run start:prod --prefix backend"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
